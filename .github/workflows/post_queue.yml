name: Post queue drainer
on:
  schedule:
    - cron: "*/20 * * * *"    # best-effort; also self-continues (see below)
  workflow_dispatch:
  # fires after the daily pipeline and review actions complete, which DO run
  workflow_run:
    workflows: ["Daily content pipeline", "Review actions (instant)"]
    types: [completed]
permissions:
  contents: write
  issues: write
  actions: write
concurrency: robopost-data
jobs:
  drain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: sudo apt-get update -qq && sudo apt-get install -y -qq fonts-dejavu
      - name: Sweep for missed review commands
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          GITHUB_REPOSITORY: "${{ github.repository }}"
          ANTHROPIC_API_KEY: "${{ secrets.ANTHROPIC_API_KEY }}"
        run: python src/github_review.py sweep || echo "sweep failed (non-fatal)"
      - name: Refill queue if commands emptied it
        env:
          ANTHROPIC_API_KEY: "${{ secrets.ANTHROPIC_API_KEY }}"
          YOUTUBE_API_KEY: "${{ secrets.YOUTUBE_API_KEY }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          GITHUB_REPOSITORY: "${{ github.repository }}"
        run: python src/refill.py || echo "refill failed (non-fatal)"
      - name: Repair missing media (re-render lost cards)
        env:
          ANTHROPIC_API_KEY: "${{ secrets.ANTHROPIC_API_KEY }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        run: python src/media_repair.py || echo "repair failed (non-fatal)"
      - name: Commit new media (must exist before issues embed it)
        run: |
          git config user.name robopost-bot
          git config user.email bot@users.noreply.github.com
          git add data media docs 2>/dev/null || git add data
          git commit -m "drainer refill media" || echo "nothing to commit"
          for i in 1 2 3; do
            if git pull --rebase; then git push && break; fi
            python src/merge_state.py
            GIT_EDITOR=true git rebase --continue || { git rebase --abort; git pull --no-rebase --no-edit -X ours; }
          done
          git push || true
      - name: Open review issues for new drafts
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          GITHUB_REPOSITORY: "${{ github.repository }}"
        run: python src/github_review.py create || echo "create failed (non-fatal)"
      - name: Post one due draft (respects 90-min spacing)
        id: post
        env:
          BLUESKY_HANDLE: "${{ secrets.BLUESKY_HANDLE }}"
          BLUESKY_APP_PASSWORD: "${{ secrets.BLUESKY_APP_PASSWORD }}"
          IG_ACCESS_TOKEN: "${{ secrets.IG_ACCESS_TOKEN }}"
          IG_USER_ID: "${{ secrets.IG_USER_ID }}"
          TIKTOK_ACCESS_TOKEN: "${{ secrets.TIKTOK_ACCESS_TOKEN }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          GITHUB_REPOSITORY: "${{ github.repository }}"
        run: python src/post_all.py
      - name: Finalize (comment results + close issues)
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          GITHUB_REPOSITORY: "${{ github.repository }}"
        run: python src/github_review.py finalize
      - name: Commit state
        if: always()
        run: |
          git config user.name robopost-bot
          git config user.email bot@users.noreply.github.com
          git add data media docs 2>/dev/null || git add data
          git commit -m "post queue drain" || echo "nothing to commit"
          for i in 1 2 3; do
            if git pull --rebase; then git push && break; fi
            python src/merge_state.py
            GIT_EDITOR=true git rebase --continue || { git rebase --abort; git pull --no-rebase --no-edit -X ours; }
          done
          git push || true
      - name: Note remaining queue
        if: always()
        run: |
          REMAIN=$(python -c "import json; d=json.load(open('data/drafts.json')); print(sum(1 for x in d if x['status']=='approved'))")
          echo "Approved drafts still queued: $REMAIN (each posts once the 90-min gap passes)."
